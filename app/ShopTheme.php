<?php
namespace App; use Illuminate\Database\Eloquent\Model; class ShopTheme extends Model { protected $guarded = array(); public $timestamps = false; protected $casts = array('options' => 'array', 'config' => 'array'); private static $default_theme; public static function defaultTheme() { if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->where('name', \App\System::_get('shop_theme_default', 'Material'))->first(); if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->firstOrFail(); } } return static::$default_theme; } public static function freshList() { $sp618c15 = realpath(app_path('..' . DIRECTORY_SEPARATOR . 'resources' . DIRECTORY_SEPARATOR . 'views' . DIRECTORY_SEPARATOR . 'shop_theme')); \App\ShopTheme::query()->get()->each(function ($sp8c4b3e) use($sp618c15) { if (!file_exists($sp618c15 . DIRECTORY_SEPARATOR . $sp8c4b3e->name . DIRECTORY_SEPARATOR . 'config.php')) { $sp8c4b3e->delete(); } }); foreach (scandir($sp618c15) as $sp717e48) { if ($sp717e48 === '.' || $sp717e48 === '..') { continue; } try { @($sp8c4b3e = (include $sp618c15 . DIRECTORY_SEPARATOR . $sp717e48 . DIRECTORY_SEPARATOR . 'config.php')); } catch (\Exception $sp7900a2) { continue; } $sp8c4b3e['config'] = array_map(function ($sp9c771b) { return $sp9c771b['value']; }, @$sp8c4b3e['options'] ?? array()); $sp409a5d = \App\ShopTheme::query()->where('name', $sp717e48)->first(); if ($sp409a5d) { $sp409a5d->description = $sp8c4b3e['description']; $sp409a5d->options = @$sp8c4b3e['options'] ?? array(); $sp409a5d->config = ($sp409a5d->config ?? array()) + $sp8c4b3e['config']; $sp409a5d->saveOrFail(); } else { if ($sp8c4b3e && isset($sp8c4b3e['description'])) { \App\ShopTheme::query()->create(array('name' => $sp717e48, 'description' => $sp8c4b3e['description'], 'options' => @$sp8c4b3e['options'] ?? array(), 'config' => $sp8c4b3e['config'])); } } } } }