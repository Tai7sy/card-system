<?php
namespace App; use Illuminate\Database\Eloquent\Model; class ShopTheme extends Model { protected $guarded = array(); public $timestamps = false; protected $casts = array('options' => 'array', 'config' => 'array'); private static $default_theme; public static function defaultTheme() { if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->where('name', \App\System::_get('shop_theme_default', 'Material'))->first(); if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->firstOrFail(); } } return static::$default_theme; } public static function freshList() { $sp19de2e = realpath(app_path('..' . DIRECTORY_SEPARATOR . 'resources' . DIRECTORY_SEPARATOR . 'views' . DIRECTORY_SEPARATOR . 'shop_theme')); \App\ShopTheme::query()->get()->each(function ($sp021d90) use($sp19de2e) { if (!file_exists($sp19de2e . DIRECTORY_SEPARATOR . $sp021d90->name . DIRECTORY_SEPARATOR . 'config.php')) { $sp021d90->delete(); } }); foreach (scandir($sp19de2e) as $sp81582e) { if ($sp81582e === '.' || $sp81582e === '..') { continue; } try { @($sp021d90 = (include $sp19de2e . DIRECTORY_SEPARATOR . $sp81582e . DIRECTORY_SEPARATOR . 'config.php')); } catch (\Exception $sp803aea) { continue; } $sp021d90['config'] = array_map(function ($spf6fea6) { return $spf6fea6['value']; }, @$sp021d90['options'] ?? array()); $spfa55db = \App\ShopTheme::query()->where('name', $sp81582e)->first(); if ($spfa55db) { $spfa55db->description = $sp021d90['description']; $spfa55db->options = @$sp021d90['options'] ?? array(); $spfa55db->config = ($spfa55db->config ?? array()) + $sp021d90['config']; $spfa55db->saveOrFail(); } else { if ($sp021d90 && isset($sp021d90['description'])) { \App\ShopTheme::query()->create(array('name' => $sp81582e, 'description' => $sp021d90['description'], 'options' => @$sp021d90['options'] ?? array(), 'config' => $sp021d90['config'])); } } } } }