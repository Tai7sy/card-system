<?php
namespace App; use Illuminate\Database\Eloquent\Model; class ShopTheme extends Model { protected $guarded = array(); public $timestamps = false; protected $casts = array('options' => 'array', 'config' => 'array'); private static $default_theme; public static function defaultTheme() { if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->where('name', \App\System::_get('shop_theme_default', 'Material'))->first(); if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->firstOrFail(); } } return static::$default_theme; } public static function freshList() { $sp253104 = realpath(app_path('..' . DIRECTORY_SEPARATOR . 'resources' . DIRECTORY_SEPARATOR . 'views' . DIRECTORY_SEPARATOR . 'shop_theme')); \App\ShopTheme::query()->get()->each(function ($spc9226b) use($sp253104) { if (!file_exists($sp253104 . DIRECTORY_SEPARATOR . $spc9226b->name . DIRECTORY_SEPARATOR . 'config.php')) { $spc9226b->delete(); } }); foreach (scandir($sp253104) as $spa80310) { if ($spa80310 === '.' || $spa80310 === '..') { continue; } try { @($spc9226b = (include $sp253104 . DIRECTORY_SEPARATOR . $spa80310 . DIRECTORY_SEPARATOR . 'config.php')); } catch (\Exception $spf95c2c) { continue; } $spc9226b['config'] = array_map(function ($spb8bcdd) { return $spb8bcdd['value']; }, @$spc9226b['options'] ?? array()); $sp8bbbdd = \App\ShopTheme::query()->where('name', $spa80310)->first(); if ($sp8bbbdd) { $sp8bbbdd->description = $spc9226b['description']; $sp8bbbdd->options = @$spc9226b['options'] ?? array(); $sp8bbbdd->config = ($sp8bbbdd->config ?? array()) + $spc9226b['config']; $sp8bbbdd->saveOrFail(); } else { if ($spc9226b && isset($spc9226b['description'])) { \App\ShopTheme::query()->create(array('name' => $spa80310, 'description' => $spc9226b['description'], 'options' => @$spc9226b['options'] ?? array(), 'config' => $spc9226b['config'])); } } } } }