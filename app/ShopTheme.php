<?php
namespace App; use Illuminate\Database\Eloquent\Model; class ShopTheme extends Model { protected $guarded = array(); public $timestamps = false; protected $casts = array('options' => 'array', 'config' => 'array'); private static $default_theme; public static function defaultTheme() { if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->where('name', \App\System::_get('shop_theme_default', 'Material'))->first(); if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->firstOrFail(); } } return static::$default_theme; } public static function freshList() { $spb1850f = realpath(app_path('..' . DIRECTORY_SEPARATOR . 'resources' . DIRECTORY_SEPARATOR . 'views' . DIRECTORY_SEPARATOR . 'shop_theme')); \App\ShopTheme::query()->get()->each(function ($sp44b25a) use($spb1850f) { if (!file_exists($spb1850f . DIRECTORY_SEPARATOR . $sp44b25a->name . DIRECTORY_SEPARATOR . 'config.php')) { $sp44b25a->delete(); } }); foreach (scandir($spb1850f) as $sp817bb5) { if ($sp817bb5 === '.' || $sp817bb5 === '..') { continue; } try { @($sp44b25a = (include $spb1850f . DIRECTORY_SEPARATOR . $sp817bb5 . DIRECTORY_SEPARATOR . 'config.php')); } catch (\Exception $spdfbc42) { continue; } $sp44b25a['config'] = array_map(function ($sp222058) { return $sp222058['value']; }, @$sp44b25a['options'] ?? array()); $sp25d3e5 = \App\ShopTheme::query()->where('name', $sp817bb5)->first(); if ($sp25d3e5) { $sp25d3e5->description = $sp44b25a['description']; $sp25d3e5->options = @$sp44b25a['options'] ?? array(); $sp25d3e5->config = ($sp25d3e5->config ?? array()) + $sp44b25a['config']; $sp25d3e5->saveOrFail(); } else { if ($sp44b25a && isset($sp44b25a['description'])) { \App\ShopTheme::query()->create(array('name' => $sp817bb5, 'description' => $sp44b25a['description'], 'options' => @$sp44b25a['options'] ?? array(), 'config' => $sp44b25a['config'])); } } } } }