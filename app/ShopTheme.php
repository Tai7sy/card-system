<?php
namespace App; use Illuminate\Database\Eloquent\Model; class ShopTheme extends Model { protected $guarded = array(); public $timestamps = false; protected $casts = array('options' => 'array', 'config' => 'array'); private static $default_theme; public static function defaultTheme() { if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->where('name', \App\System::_get('shop_theme_default', 'Material'))->first(); if (!static::$default_theme) { static::$default_theme = ShopTheme::query()->firstOrFail(); } } return static::$default_theme; } public static function freshList() { $sp650cb7 = realpath(app_path('..' . DIRECTORY_SEPARATOR . 'resources' . DIRECTORY_SEPARATOR . 'views' . DIRECTORY_SEPARATOR . 'shop_theme')); \App\ShopTheme::query()->get()->each(function ($sp4cb5d8) use($sp650cb7) { if (!file_exists($sp650cb7 . DIRECTORY_SEPARATOR . $sp4cb5d8->name . DIRECTORY_SEPARATOR . 'config.php')) { $sp4cb5d8->delete(); } }); foreach (scandir($sp650cb7) as $sp2ef7d2) { if ($sp2ef7d2 === '.' || $sp2ef7d2 === '..') { continue; } try { @($sp4cb5d8 = (include $sp650cb7 . DIRECTORY_SEPARATOR . $sp2ef7d2 . DIRECTORY_SEPARATOR . 'config.php')); } catch (\Exception $spa356ee) { continue; } $sp4cb5d8['config'] = array_map(function ($sp286f69) { return $sp286f69['value']; }, @$sp4cb5d8['options'] ?? array()); $spaf9363 = \App\ShopTheme::query()->where('name', $sp2ef7d2)->first(); if ($spaf9363) { $spaf9363->description = $sp4cb5d8['description']; $spaf9363->options = @$sp4cb5d8['options'] ?? array(); $spaf9363->config = ($spaf9363->config ?? array()) + $sp4cb5d8['config']; $spaf9363->saveOrFail(); } else { if ($sp4cb5d8 && isset($sp4cb5d8['description'])) { \App\ShopTheme::query()->create(array('name' => $sp2ef7d2, 'description' => $sp4cb5d8['description'], 'options' => @$sp4cb5d8['options'] ?? array(), 'config' => $sp4cb5d8['config'])); } } } } }