<?php
namespace App\Mail; use Illuminate\Bus\Queueable; use Illuminate\Mail\Mailable; use Illuminate\Queue\SerializesModels; use Illuminate\Contracts\Queue\ShouldQueue; class OrderShipped extends Mailable { use Queueable, SerializesModels; public $tries = 3; public $timeout = 20; public $order_no; public $order_url; public $card_msg; public $cards_txt; public function __construct($sp7cc18f, $spccbb20, $sp9e234a) { $this->order_no = $sp7cc18f->order_no; $this->order_url = config('app.url') . route('pay.result', array($sp7cc18f->order_no), false); $this->card_msg = $spccbb20; $this->cards_txt = $sp9e234a; } public function build() { return $this->subject('订单提醒(#' . $this->order_no . ')-' . config('app.name'))->view('emails.order'); } }