<?php
namespace App\Mail; use Illuminate\Bus\Queueable; use Illuminate\Mail\Mailable; use Illuminate\Queue\SerializesModels; use Illuminate\Contracts\Queue\ShouldQueue; class OrderShipped extends Mailable { use Queueable, SerializesModels; public $order_no; public $order_url; public $card_msg; public $cards_txt; public function __construct($spcd0b15, $sp375f4f, $sp55b6cb) { $this->order_no = $spcd0b15->order_no; $this->order_url = config('app.url') . route('pay.result', array($spcd0b15->order_no), false); $this->card_msg = $sp375f4f; $this->cards_txt = $sp55b6cb; } public function build() { return $this->subject('订单提醒(#' . $this->order_no . ')-' . config('app.name'))->view('emails.order'); } }