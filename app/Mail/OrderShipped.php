<?php
namespace App\Mail; use Illuminate\Bus\Queueable; use Illuminate\Mail\Mailable; use Illuminate\Queue\SerializesModels; use Illuminate\Contracts\Queue\ShouldQueue; class OrderShipped extends Mailable { use Queueable, SerializesModels; public $order_no; public $order_url; public $card_msg; public $cards_txt; public function __construct($sp79a792, $sp19f776, $sp41a287) { $this->order_no = $sp79a792->order_no; $this->order_url = config('app.url') . route('pay.result', array($sp79a792->order_no), false); $this->card_msg = $sp19f776; $this->cards_txt = $sp41a287; } public function build() { return $this->subject('订单提醒(#' . $this->order_no . ')-' . config('app.name'))->view('emails.order'); } }