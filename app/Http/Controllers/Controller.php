<?php
namespace App\Http\Controllers; use App\System; use App\Library\Geetest; use Illuminate\Foundation\Bus\DispatchesJobs; use Illuminate\Http\Response; use Illuminate\Routing\Controller as BaseController; use Illuminate\Foundation\Validation\ValidatesRequests; use Illuminate\Foundation\Auth\Access\AuthorizesRequests; use Illuminate\Http\Request; use Illuminate\Support\Facades\Auth; class Controller extends BaseController { use AuthorizesRequests, DispatchesJobs, ValidatesRequests; public function getCaptcha() { $sp4e6985 = System::_get('vcode_driver'); if ($sp4e6985 === 'code') { return response(array()); } elseif ($sp4e6985 === 'geetest') { return response(Geetest\API::get()); } elseif ($sp4e6985 === 'recaptcha') { } return response(array(), Response::HTTP_NOT_IMPLEMENTED); } function validateCaptcha(Request $sp510ef3) { $sp4e6985 = System::_get('vcode_driver'); if ($sp4e6985 === 'code') { $this->validate($sp510ef3, array('captcha.key' => 'required|string', 'captcha.code' => 'required|captcha_api:' . $sp510ef3->input('captcha.key'))); } elseif ($sp4e6985 === 'geetest') { $this->validate($sp510ef3, array('captcha.a' => 'required|string', 'captcha.b' => 'required|string', 'captcha.c' => 'required|string', 'captcha.d' => 'required|string')); if (!Geetest\API::verify($sp510ef3->input('captcha.a'), $sp510ef3->input('captcha.b'), $sp510ef3->input('captcha.c'), $sp510ef3->input('captcha.d'))) { throw \Illuminate\Validation\ValidationException::withMessages(array('captcha' => array(trans('validation.captcha')))); } } elseif ($sp4e6985 === 'recaptcha') { $this->validate($sp510ef3, array('captcha.t' => 'required|string')); } } function authQuery(Request $sp510ef3, $sp4f90dd, $sp8bd94f = 'user_id', $sp238afd = 'user_id') { return $sp4f90dd::where($sp8bd94f, \Auth::id()); } protected function getUserId(Request $sp510ef3, $sp238afd = 'user_id') { return \Auth::id(); } protected function getUserIdOrFail(Request $sp510ef3, $sp238afd = 'user_id') { $sp3546ff = self::getUserId($sp510ef3, $sp238afd); if ($sp3546ff) { return $sp3546ff; } else { throw new \Exception('参数缺少 ' . $sp238afd); } } protected function getUser(Request $sp510ef3) { return \Auth::getUser(); } protected function checkIsInMaintain() { if (System::_getInt('maintain') === 1) { $sp101017 = System::_get('maintain_info'); echo view('message', array('title' => '维护中', 'message' => $sp101017)); die; } } protected function msg($sp8276fc, $spc8e803 = null, $sp8f933b = null) { return view('message', array('message' => $sp8276fc, 'title' => $spc8e803, 'exception' => $sp8f933b)); } }