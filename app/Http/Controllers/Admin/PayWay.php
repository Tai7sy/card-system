<?php
namespace App\Http\Controllers\Admin; use App\Library\Response; use Illuminate\Http\Request; use App\Http\Controllers\Controller; class PayWay extends Controller { function get(Request $sp510ef3) { $spb02a8e = (int) $sp510ef3->input('current_page', 1); $sp95f730 = (int) $sp510ef3->input('per_page', 20); $sp90af04 = \App\PayWay::orderBy('sort')->where('type', $sp510ef3->input('type')); $spebf2d9 = $sp510ef3->input('search', false); $spac3bf6 = $sp510ef3->input('val', false); if ($spebf2d9 && $spac3bf6) { if ($spebf2d9 == 'simple') { return Response::success($sp90af04->get(array('id', 'name'))); } elseif ($spebf2d9 == 'id') { $sp90af04->where('id', $spac3bf6); } else { $sp90af04->where($spebf2d9, 'like', '%' . $spac3bf6 . '%'); } } $sp73556e = $sp510ef3->input('enabled'); if (strlen($sp73556e)) { $sp90af04->whereIn('enabled', explode(',', $sp73556e)); } $spe24324 = $sp90af04->paginate($sp95f730, array('*'), 'page', $spb02a8e); return Response::success($spe24324); } function edit(Request $sp510ef3) { $this->validate($sp510ef3, array('id' => 'required|integer', 'type' => 'required|integer|between:1,2', 'name' => 'required|string', 'sort' => 'required|integer', 'channels' => 'required|string', 'enabled' => 'required|integer|between:0,3')); $sp258ace = (int) $sp510ef3->post('id'); $specafcb = \App\PayWay::find($sp258ace); if (!$specafcb) { if (\App\PayWay::where('name', $sp510ef3->post('name'))->exists()) { return Response::fail('名称已经存在'); } $specafcb = new \App\PayWay(); } else { if (\App\PayWay::where('name', $sp510ef3->post('name'))->where('id', '!=', $specafcb->id)->exists()) { return Response::fail('名称已经存在'); } } $specafcb->type = (int) $sp510ef3->post('type'); $specafcb->name = $sp510ef3->post('name'); $specafcb->sort = (int) $sp510ef3->post('sort'); $specafcb->img = $sp510ef3->post('img'); $specafcb->channels = @json_decode($sp510ef3->post('channels')) ?? array(); $specafcb->comment = $sp510ef3->post('comment'); $specafcb->enabled = (int) $sp510ef3->post('enabled'); $specafcb->saveOrFail(); return Response::success(); } function enable(Request $sp510ef3) { $this->validate($sp510ef3, array('ids' => 'required|string', 'enabled' => 'required|integer|between:0,3')); $sp3cf93f = $sp510ef3->post('ids'); $sp73556e = (int) $sp510ef3->post('enabled'); \App\PayWay::whereIn('id', explode(',', $sp3cf93f))->update(array('enabled' => $sp73556e)); return Response::success(); } function sort(Request $sp510ef3) { $this->validate($sp510ef3, array('id' => 'required|integer')); $sp258ace = (int) $sp510ef3->post('id'); $specafcb = \App\PayWay::findOrFail($sp258ace); $specafcb->sort = (int) $sp510ef3->post('sort'); $specafcb->save(); return Response::success(); } function delete(Request $sp510ef3) { $this->validate($sp510ef3, array('ids' => 'required|string')); $sp3cf93f = $sp510ef3->post('ids'); \App\PayWay::whereIn('id', explode(',', $sp3cf93f))->delete(); return Response::success(); } }