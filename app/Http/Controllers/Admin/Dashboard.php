<?php
namespace App\Http\Controllers\Admin; use App\Library\CurlRequest; use App\Library\Response; use App\Order; use Illuminate\Http\Request; use App\Http\Controllers\Controller; use Illuminate\Support\Facades\Auth; use Illuminate\Support\Facades\Cache; class Dashboard extends Controller { function index(Request $spccbfb6) { $sp9e97ea = array('today' => array('count' => 0, 'paid' => 0, 'profit' => 0), 'yesterday' => array('count' => 0, 'paid' => 0, 'profit' => 0)); $spa46b78 = Order::whereUserId(\Auth::Id())->whereDate('paid_at', \Carbon\Carbon::now()->toDateString())->where(function ($sp479bde) { $sp479bde->where('status', Order::STATUS_PAID)->orWhere('status', Order::STATUS_SUCCESS); })->selectRaw('COUNT(*) as `count`,SUM(`paid`) as `paid`,SUM(`paid`-`sms_price`-`cost`-`fee`) as `profit`')->get()->toArray(); $sp80b66c = Order::whereUserId(\Auth::Id())->whereDate('paid_at', \Carbon\Carbon::yesterday()->toDateString())->where(function ($sp479bde) { $sp479bde->where('status', Order::STATUS_PAID)->orWhere('status', Order::STATUS_SUCCESS); })->selectRaw('COUNT(*) as `count`,SUM(`paid`) as `paid`,SUM(`paid`-`sms_price`-`cost`-`fee`) as `profit`')->get()->toArray(); if (isset($spa46b78[0]) && isset($spa46b78[0]['count'])) { $sp9e97ea['today'] = array('count' => (int) $spa46b78[0]['count'], 'paid' => (int) $spa46b78[0]['paid'], 'profit' => (int) $spa46b78[0]['profit']); } if (isset($sp80b66c[0]) && isset($sp80b66c[0]['count'])) { $sp9e97ea['yesterday'] = array('count' => (int) $sp80b66c[0]['count'], 'paid' => (int) $sp80b66c[0]['paid'], 'profit' => (int) $sp80b66c[0]['profit']); } $sp9e97ea['need_ship_count'] = Order::whereUserId(\Auth::Id())->where('status', Order::STATUS_PAID)->count(); $sp9e97ea['login'] = \App\Log::where('action', \App\Log::ACTION_LOGIN)->latest()->first(); return Response::success($sp9e97ea); } function clearCache() { if (function_exists('opcache_reset')) { opcache_reset(); } try { \Artisan::call('cache:clear'); \Artisan::call('route:cache'); \Artisan::call('config:cache'); } catch (\Throwable $spdfbc42) { return Response::fail($spdfbc42->getMessage()); } return Response::success(); } function version() { return Response::success(array('version' => config('app.version'))); } function logsToken() { $sp948af1 = md5(random_bytes(128)); Cache::put($sp948af1, Auth::getUser(), 15); return response(array('token' => $sp948af1)); } function logsView(Request $spccbfb6, $sp948af1) { if ($sp91cc0b = Cache::get($sp948af1)) { Cache::put($sp948af1, $sp91cc0b, 15); return (new \Rap2hpoutre\LaravelLogViewer\LogViewerController())->index(); } else { throw new \Symfony\Component\HttpKernel\Exception\UnauthorizedHttpException('logs-token'); } } function checkUpdate() { return Response::success(@json_decode(CurlRequest::get('https://raw.githubusercontent.com/Tai7sy/card-system/master/.version'), true)); } }