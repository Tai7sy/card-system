<?php
namespace App\Http\Controllers\Admin; use App\Library\CurlRequest; use App\Library\Response; use App\Order; use Illuminate\Http\Request; use App\Http\Controllers\Controller; use Illuminate\Support\Facades\Auth; use Illuminate\Support\Facades\Cache; class Dashboard extends Controller { function index(Request $sp7f7104) { $spc16ec8 = array('today' => array('count' => 0, 'paid' => 0, 'profit' => 0), 'yesterday' => array('count' => 0, 'paid' => 0, 'profit' => 0)); $spb8728c = Order::whereUserId(\Auth::Id())->whereDate('paid_at', \Carbon\Carbon::now()->toDateString())->where(function ($spbec213) { $spbec213->where('status', Order::STATUS_PAID)->orWhere('status', Order::STATUS_SUCCESS); })->selectRaw('COUNT(*) as `count`,SUM(`paid`) as `paid`,SUM(`paid`-`sms_price`-`cost`-`fee`) as `profit`')->get()->toArray(); $spe7ecd9 = Order::whereUserId(\Auth::Id())->whereDate('paid_at', \Carbon\Carbon::yesterday()->toDateString())->where(function ($spbec213) { $spbec213->where('status', Order::STATUS_PAID)->orWhere('status', Order::STATUS_SUCCESS); })->selectRaw('COUNT(*) as `count`,SUM(`paid`) as `paid`,SUM(`paid`-`sms_price`-`cost`-`fee`) as `profit`')->get()->toArray(); if (isset($spb8728c[0]) && isset($spb8728c[0]['count'])) { $spc16ec8['today'] = array('count' => (int) $spb8728c[0]['count'], 'paid' => (int) $spb8728c[0]['paid'], 'profit' => (int) $spb8728c[0]['profit']); } if (isset($spe7ecd9[0]) && isset($spe7ecd9[0]['count'])) { $spc16ec8['yesterday'] = array('count' => (int) $spe7ecd9[0]['count'], 'paid' => (int) $spe7ecd9[0]['paid'], 'profit' => (int) $spe7ecd9[0]['profit']); } $spc16ec8['need_ship_count'] = Order::whereUserId(\Auth::Id())->where('status', Order::STATUS_PAID)->count(); $spc16ec8['login'] = \App\Log::where('action', \App\Log::ACTION_LOGIN)->latest()->first(); return Response::success($spc16ec8); } function clearCache() { if (function_exists('opcache_reset')) { opcache_reset(); } try { \Artisan::call('cache:clear'); \Artisan::call('route:cache'); \Artisan::call('config:cache'); } catch (\Throwable $spd54c56) { return Response::fail($spd54c56->getMessage()); } return Response::success(); } function version() { return Response::success(array('version' => config('app.version'))); } function logsToken() { $spd5a5b2 = md5(random_bytes(128)); Cache::put($spd5a5b2, Auth::getUser(), 15); return response(array('token' => $spd5a5b2)); } function logsView(Request $sp7f7104, $spd5a5b2) { if ($sp216502 = Cache::get($spd5a5b2)) { Cache::put($spd5a5b2, $sp216502, 15); return (new \Rap2hpoutre\LaravelLogViewer\LogViewerController())->index(); } else { throw new \Symfony\Component\HttpKernel\Exception\UnauthorizedHttpException('logs-token'); } } function checkUpdate() { return Response::success(@json_decode(CurlRequest::get('https://raw.githubusercontent.com/Tai7sy/card-system/master/.version'), true)); } }