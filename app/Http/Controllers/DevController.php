<?php
namespace App\Http\Controllers; use App\System; use Illuminate\Http\Request; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Mail; class DevController extends Controller { private function check_readable_r($spca62ff) { if (is_dir($spca62ff)) { if (is_readable($spca62ff)) { $spec7b45 = scandir($spca62ff); foreach ($spec7b45 as $spf76260) { if ($spf76260 != '.' && $spf76260 != '..') { if (!self::check_readable_r($spca62ff . '/' . $spf76260)) { return false; } else { continue; } } } echo $spca62ff . '   ...... <span style="color: green">R</span><br>'; return true; } else { echo $spca62ff . '   ...... <span style="color: red">R</span><br>'; return false; } } else { if (file_exists($spca62ff)) { return is_readable($spca62ff); } } echo $spca62ff . '   ...... 文件不存在<br>'; return false; } private function check_writable_r($spca62ff) { if (is_dir($spca62ff)) { if (is_writable($spca62ff)) { $spec7b45 = scandir($spca62ff); foreach ($spec7b45 as $spf76260) { if ($spf76260 != '.' && $spf76260 != '..') { if (!self::check_writable_r($spca62ff . '/' . $spf76260)) { return false; } else { continue; } } } echo $spca62ff . '   ...... <span style="color: green">W</span><br>'; return true; } else { echo $spca62ff . '   ...... <span style="color: red">W</span><br>'; return false; } } else { if (file_exists($spca62ff)) { return is_writable($spca62ff); } } echo $spca62ff . '   ...... 文件不存在<br>'; return false; } private function checkPathPermission($sp65b8bf) { self::check_readable_r($sp65b8bf); self::check_writable_r($sp65b8bf); } public function install() { $sp77e4ab = array(); @ob_start(); self::checkPathPermission(base_path('storage')); self::checkPathPermission(base_path('bootstrap/cache')); $sp77e4ab['permission'] = @ob_get_clean(); return view('install', array('var' => $sp77e4ab)); } public function test(Request $sp054aa0) { } }