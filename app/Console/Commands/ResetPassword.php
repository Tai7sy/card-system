<?php
namespace App\Console\Commands; use Illuminate\Console\Command; class ResetPassword extends Command { protected $signature = 'reset:password {email} {password}'; protected $description = 'Reset the password of user
Usage: php artisan reset:password user@email.com'; public function __construct() { parent::__construct(); } public function handle() { $sp90b45f = $this->argument('email'); if (!$sp90b45f) { $this->warn('please input the user\'s email
'); return false; } $spd05c92 = \App\User::where('email', $sp90b45f)->first(); if (!$spd05c92) { $this->warn("can't find the user: {$sp90b45f} \nplease input the user's email\n"); return false; } $sp86b1f2 = $this->argument('password'); $spd05c92->password = bcrypt($sp86b1f2); $spd05c92->save(); $this->info("the password of '{$sp90b45f}' has been set to {$sp86b1f2}\n"); return true; } }