<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $sp9f1705) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $sp696863) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $spc79160 = System::_getInt('order_clean_unpay_day', 7); $sp9f1705->call(function () use($spc79160) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$spc79160} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$spc79160))->delete(); $sp4daef6 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp4daef6; })->dailyAt('01:00'); } $sp9f1705->call(function () { $spc79160 = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$spc79160} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$spc79160))->forceDelete(); $sp4daef6 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp4daef6; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }