<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $spfd9ebd) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $spbd4f27) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $sp66f4c4 = System::_getInt('order_clean_unpay_day', 7); $spfd9ebd->call(function () use($sp66f4c4) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$sp66f4c4} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$sp66f4c4))->delete(); $sp4e57b0 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp4e57b0; })->dailyAt('01:00'); } $spfd9ebd->call(function () { $sp66f4c4 = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$sp66f4c4} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$sp66f4c4))->forceDelete(); $sp4e57b0 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp4e57b0; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }