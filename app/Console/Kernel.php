<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $sp451d1b) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $sp54a0c6) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $sp4f640c = System::_getInt('order_clean_unpay_day', 7); $sp451d1b->call(function () use($sp4f640c) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$sp4f640c} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$sp4f640c))->delete(); $spb56031 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $spb56031; })->dailyAt('01:00'); } $sp451d1b->call(function () { $sp4f640c = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$sp4f640c} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$sp4f640c))->forceDelete(); $spb56031 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $spb56031; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }