<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $spd458a9) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $spd118f7) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $speb0111 = System::_getInt('order_clean_unpay_day', 7); $spd458a9->call(function () use($speb0111) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$speb0111} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$speb0111))->delete(); $spa7c069 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $spa7c069; })->dailyAt('01:00'); } $spd458a9->call(function () { $speb0111 = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$speb0111} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$speb0111))->forceDelete(); $spa7c069 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $spa7c069; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }