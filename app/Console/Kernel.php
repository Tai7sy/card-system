<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $sp3925e3) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $spd54c56) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $sp2c608d = System::_getInt('order_clean_unpay_day', 7); $sp3925e3->call(function () use($sp2c608d) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$sp2c608d} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$sp2c608d))->delete(); $spba9aaf = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $spba9aaf; })->dailyAt('01:00'); } $sp3925e3->call(function () { $sp2c608d = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$sp2c608d} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$sp2c608d))->forceDelete(); $spba9aaf = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $spba9aaf; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }