<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $sp5c7212) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $spd3b3eb) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $spac3a98 = System::_getInt('order_clean_unpay_day', 7); $sp5c7212->call(function () use($spac3a98) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$spac3a98} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$spac3a98))->delete(); $sp8276fc = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp8276fc; })->dailyAt('01:00'); } $sp5c7212->call(function () { $spac3a98 = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$spac3a98} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$spac3a98))->forceDelete(); $sp8276fc = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp8276fc; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }