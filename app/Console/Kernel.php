<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $spbe61da) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $spf745ad) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $sp9c9b8d = System::_getInt('order_clean_unpay_day', 7); $spbe61da->call(function () use($sp9c9b8d) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$sp9c9b8d} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$sp9c9b8d))->delete(); $spe0b8c9 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $spe0b8c9; })->dailyAt('01:00'); } $spbe61da->call(function () { $sp9c9b8d = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$sp9c9b8d} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$sp9c9b8d))->forceDelete(); $spe0b8c9 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $spe0b8c9; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }