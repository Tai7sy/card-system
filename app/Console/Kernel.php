<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $sp38a9c3) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $sp4b79b8) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $sp0bb0c0 = System::_getInt('order_clean_unpay_day', 7); $sp38a9c3->call(function () use($sp0bb0c0) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$sp0bb0c0} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$sp0bb0c0))->delete(); $sp15b8e2 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp15b8e2; })->dailyAt('01:00'); } $sp38a9c3->call(function () { $sp0bb0c0 = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$sp0bb0c0} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$sp0bb0c0))->forceDelete(); $sp15b8e2 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp15b8e2; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }