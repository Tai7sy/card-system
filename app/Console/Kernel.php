<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $spfef638) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $sp8e3e91) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $sp2b46ec = System::_getInt('order_clean_unpay_day', 7); $spfef638->call(function () use($sp2b46ec) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$sp2b46ec} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$sp2b46ec))->delete(); $sp24c224 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp24c224; })->dailyAt('01:00'); } $spfef638->call(function () { $sp2b46ec = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$sp2b46ec} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$sp2b46ec))->forceDelete(); $sp24c224 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp24c224; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }