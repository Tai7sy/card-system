<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $spcdcc55) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $sp45f5f1) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $spcb4d2f = System::_getInt('order_clean_unpay_day', 7); $spcdcc55->call(function () use($spcb4d2f) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$spcb4d2f} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$spcb4d2f))->delete(); $sp478b36 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp478b36; })->dailyAt('01:00'); } $spcdcc55->call(function () { $spcb4d2f = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$spcb4d2f} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$spcb4d2f))->forceDelete(); $sp478b36 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp478b36; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }