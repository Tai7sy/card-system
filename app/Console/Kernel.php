<?php
namespace App\Console; use App\System; use Carbon\Carbon; use Illuminate\Console\Scheduling\Schedule; use Illuminate\Foundation\Console\Kernel as ConsoleKernel; use Illuminate\Support\Facades\DB; use Illuminate\Support\Facades\Log; use Illuminate\Support\Facades\Schema; class Kernel extends ConsoleKernel { protected $commands = array(); protected function schedule(Schedule $spb0964c) { if (!app()->runningInConsole()) { return; } try { System::_init(); } catch (\Throwable $sped8699) { return; } if (System::_getInt('order_clean_unpay_open') === 1) { $spf12ece = System::_getInt('order_clean_unpay_day', 7); $spb0964c->call(function () use($spf12ece) { echo '[' . date('Y-m-d H:i:s') . "] cleaning unpaid orders({$spf12ece} days ago)...\n"; \App\Order::where('status', \App\Order::STATUS_UNPAY)->where('created_at', '<', (new Carbon())->addDays(-$spf12ece))->delete(); $sp8f0c89 = '[' . date('Y-m-d H:i:s') . '] unpaid-orders cleaned 
'; echo $sp8f0c89; })->dailyAt('01:00'); } $spb0964c->call(function () { $spf12ece = 7; echo '[' . date('Y-m-d H:i:s') . "] cleaning deleted cards({$spf12ece} days ago)...\n"; \App\Card::onlyTrashed()->where('deleted_at', '<', (new Carbon())->addDays(-$spf12ece))->forceDelete(); $sp8f0c89 = '[' . date('Y-m-d H:i:s') . '] deleted-cards cleaned
'; echo $sp8f0c89; })->dailyAt('02:00'); } protected function commands() { $this->load(__DIR__ . '/Commands'); require base_path('routes/console.php'); } }